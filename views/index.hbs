<h2>List of Books</h2>

<form method="GET" action="/search">
    <div>
        <label for="book_cat">Books starting with</label>
    </div>

        {{#each bookletter}}
            <a href="books/{{this}}"><button type="button" class="btn btn-primary" id="t1" name="q" value="{{this}}">
            {{ this }}

            </button></a>
            
        {{/each}}
    </select>

   
</form>

